#include <stdio.h>
#include <stdlib.h>

// Command line options to handle:
// -n problemsize
// -D               (show default values)
// -h               (help)
// -I init_type     (fast/rand)
// -m maxnum        (max random no)
// -P print_switch  (0/1)

void main() {}
