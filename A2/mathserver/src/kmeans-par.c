#include <stdio.h>
#include <stdlib.h>

// Command line options to handle:
// -f file          (Specify the file containing the problem data)
// -k clusters      (Specify the number of clusters)

void main() {}
